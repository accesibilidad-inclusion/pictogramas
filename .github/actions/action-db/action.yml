name: 'Update pictos on clients'
description: 'update yarn pictos dependence'
runs:
  using: "composite"
  steps: 
    - run: node script.js
      working-directory: /var/www/pictos/qa/pictos-backend/
      shell: bash