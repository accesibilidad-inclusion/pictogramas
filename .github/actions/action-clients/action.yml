name: 'Update pictos on clients'
description: 'update yarn pictos dependence'
runs:
  using: "composite"
  steps: 
    - run: yarn upgrade pictos --latest
      working-directory: /var/www/pictos/pictos-admin-client/
      shell: bash
    - run: yarn upgrade pictos --latest
      working-directory: /var/www/pictos/pictos-web-app/
      shell: bash